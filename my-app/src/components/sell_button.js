import React, {Component} from 'react'
import {StyledButton} from './login_buttons'

class SellButton extends Component{
    render(){
        return(
            <button className='login-button'>Sell</button>
        )
    }
}

export default SellButton;